import { LangDict } from './default'

// Intentionally empty until a translator fills these. The scaffold's entries were
// keyed by index and our strings changed what each index means, so keeping them
// would have mistranslated rather than left a gap.
export default {} as Record<string, LangDict>
